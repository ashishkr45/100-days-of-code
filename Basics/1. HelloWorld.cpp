#include <iostream>
// iostream is the cpp library which include the functions of input and output.
int main()
{
    // here, << - insertion operatior and >> - extration operator.

    std::cout << "Hello World\n";

    // "std::count" is used to display output.
    // here std is name space standrad and std stand for standrad
    // if we dont want to mention this std evytime we print we can mention, "using namespace std" below, "#include <iostream>" line.

    return 0;
    // this representates the thi main function is not returning anything.
}